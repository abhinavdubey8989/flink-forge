
# How to add enviroment vars in intellij
- Use-case : read the appropriate properties file (application-local.properties)
- How to
  - Run > Edit configurations > Environment variables > Add the vars here (eg : `APP_ENV=local`)
  - Apply > Save